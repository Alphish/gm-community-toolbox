@script <Layer> Layer utilities

@func <layer_destroy_sprites>

@section Example

The following code removes all sprite elements from the Decorations layer.

```gml
layer_destroy_sprites("Decorations");
```

@update 24.11.0
Created a function to destroy all sprite elements in a given layer.

@func <layer_destroy_text>

@section Example

The following code removes all text elements from the Instructions layer.

```gml
layer_destroy_text("Instructions");
```

@update 24.11.0
Created a function to destroy all text elements in a given layer.

@func <layer_destroy_sequences>

@section Example

The following code removes all sequence elements from the Intro layer.

```gml
layer_destroy_sequences("Intro");
```

@update 24.11.0
Created a function to destroy all sequence elements in a given layer.

@func <layer_destroy_backgrounds>

@section Example

The following code removes all background elements from the Background layer.

```gml
layer_destroy_backgrounds("Background");
```

@update 24.11.0
Created a function to destroy all background elements in a given layer.

@func <layer_destroy_tilemaps>

@section Example

The following code removes all tilemap elements from the Floor layer.

```gml
layer_destroy_tilemaps("Floor");
```

@update 24.11.0
Created a function to destroy all tilemap elements in a given layer.
